package com.parkingengine.persistance.impl;

import static org.junit.Assert.assertEquals;

import org.joda.time.DateTime;
import org.joda.time.LocalTime;
import org.joda.time.format.DateTimeFormat;
import org.joda.time.format.DateTimeFormatter;
import org.junit.Test;

public class TestingGrounds {
	
  /*
	@Test
	public void testJodaLocalTimeParser(){
		
		DateTimeFormatter hoursMinFormatter = DateTimeFormat.forPattern("HH:mm");
		System.out.println(DateTime.now().toString(hoursMinFormatter));
		
		LocalTime localTime = LocalTime.parse("04:00",hoursMinFormatter);
		System.out.println(localTime.toString());
		assertEquals(1,1);
		
		
	}
	*/

}
